import 'package:flutter/material.dart';

class HomeScreenSysAdmin extends StatelessWidget {
  static const String route = 'HomeScreenSysAdmin';
  const HomeScreenSysAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'HomeScreenSysAdmin'
        ),
      ),
    );
  }
}
